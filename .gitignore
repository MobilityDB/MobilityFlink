.DS_Store
target/
**/target/
*.class
.idea/
.vscode
*.jar

# Maven build output
benchmark/target/
# Python generator caches (tools/codegen_facades.py runs at build time)
__pycache__/
**/__pycache__/


# Derived generator input — the MEOS catalog is staged in CI from upstream
# MobilityDB master (provision-meos), never committed.
tools/meos-idl.json
# The JVM generator JMEOS owns, staged beside the catalog by the refresh chain
# and by CI. Vendoring it is what let three copies drift apart.
tools/codegen_jvm.py
tools/codegen_spark_udfs.py

# tools/refresh-from-master.sh scratch (sibling clones + libmeos prefix).
.meos-chain/
